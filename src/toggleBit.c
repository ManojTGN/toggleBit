#include "toggleBit.h"

typedef struct node {
    Number* number;
    struct node* next;
    struct node* prev;
} NODE;NODE* HEAD = NULL;

typedef struct number{
    uint64_t data;
    uint8_t multiplier;
}Number;



bool toggleBit(char* number){
    return false;
}

char* getValue(){
    return "0";
}
